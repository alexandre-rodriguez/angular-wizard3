export enum STEP_STATE {
  normal = 'normal',
  disabled = 'disabled',
  error = 'error',
  hidden = 'hidden'
}

export enum STEP_STATUS {
  untouched = 'untouched',
  done = 'done',
  active = 'active',
  complete = 'complete'
}

export enum STEP_DIRECTIN {
  forward = 'forward',
  backward = 'backward',
  none = 'none'
}

export enum STEP_POSITION {
  first = 'first',
  final = 'final',
  middle = 'middle'
}
